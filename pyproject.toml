# contents of a minimal pyproject.toml 

[build-system] 
requires = ["hatchling"] 
build-backend = "hatchling.build" 

[project] 
name = "example_package_gdang2" 
version = "0.1.0" 
dependencies = ["numpy"]
readme="README.md"

[project.optional-dependencies]
test = ["pytest"]

